// This shows a simple example of how to archive the build output artifacts.
node ("docker_host") {
    stage("Deploy site"){
   
    sh " cd /opt/containerd/data/apache_wordpress_mysql; docker-compose up -d"
    }

}
